#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../PrettyCSS/bin/prettycss" "$@"
  ret=$?
else 
  node  "$basedir/../PrettyCSS/bin/prettycss" "$@"
  ret=$?
fi
exit $ret
